#include <iostream>
#include<math.h>
using namespace std;
int main() {
    int a,b;
    cin>>a>>b;
    int maxval=max(a,b);
    for(int i=maxval;i<=a*b;i++)
    {
        if(i%a==0 && i%b==0)
        {
            cout<<i;
            break;
        }
    }
    return 0;
}
